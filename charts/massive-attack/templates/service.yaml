apiVersion: v1
kind: Service
metadata:
  name: {{ include "massive-attack.fullname" . }}
  labels:
    {{- include "massive-attack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "massive-attack.selectorLabels" . | nindent 4 }}
